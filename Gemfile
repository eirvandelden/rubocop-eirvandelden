source "https://gem.coop"

ruby file: ".ruby-version"

gemspec

gem "rake", require: false

# Projects that use Capybara add the matching config; the gem only needs it to test it.
gem "rubocop-capybara", require: false
